# BarTender GUI

This repository contains a CustomTkinter GUI for printing labels through BarTender.

## Running

Install dependencies and launch the application:

```bash
pip install -r requirements.txt
python main.py
```

## Packaging

When building with PyInstaller, use `main.py` as the entry point and bundle any required BarTender template/Excel resource files alongside the executable.
